# AI Agent Guidelines - Modulos Project

Welcome, Agent. This document provides the necessary context and technical guidelines to work efficiently on the **Modulos** project.

## 🚀 Project Overview
**Modulos** is a scalable user management system built with **.NET 8.0**. It features a modern, secure architecture with a clear separation between the Backend (API) and Frontend (Client).

## 🏗 Architecture
- **Modulos.API**: 
  - ASP.NET Core Web API.
  - **Auth**: ASP.NET Core Identity + JWT.
  - **Data**: Entity Framework Core with SQL Server.
  - **DTOs**: Located in `Modulos.API/DTOs`.
- **Modulos.Client**: 
  - ASP.NET Core MVC.
  - Consumes the API using `HttpClient`.
  - Implements a custom authentication provider to handle JWT tokens.
- **Modulos.Tests**: Unit and integration tests.

## 🎨 UI/UX Standards
We aim for a **Premium/Modern** look:
- **Design System**: Glassmorphism (blur effects, semi-transparent backgrounds).
- **Styling**: Bootstrap 5 + Custom CSS variables in `Modulos.Client/wwwroot/css/site.css`.
- **Typography**: `Inter` (Google Fonts).
- **Animations**: Subtle fade-ins and scale transitions for buttons.

## 🔒 Security Patterns
- **Role-Based Access Control (RBAC)**: `SuperAdmin`, `Admin`, `User`.
- **Password Flow**: New users have a mandatory `MustChangePassword` flag.
- **Validation**: 
  - DNI must be 8 numeric characters.
  - Password complexity is enforced.
  - No pasting allowed in "Confirm Password" fields.
- **API Communication**: All requests to the API must include the JWT token in the `Authorization` header.

## 🧱 UI Component Standards
- **Tables**: Use **DataTables.net** for all data grids to provide searching, pagination, and sorting.
  - **Styles**: Use the Bootstrap 5 integration for DataTables.
  - **Language**: Always initialize with Spanish labels.
  - **Glassmorphism**: Ensure the DataTable container maintains the project's glassmorphism style.

## 🛠 Useful Commands
- **API**: `dotnet run --project Modulos.API`
- **Client**: `dotnet run --project Modulos.Client`
- **Database**: `dotnet ef database update --project Modulos.API`
- **Tests**: `dotnet test`

## 📝 Agent Instructions
1. **Maintain Quality**: Ensure all new UI components follow the Glassmorphism style.
2. **Update Docs**: If you add a major feature, update `FEATURES.md` and `README.md`.
3. **Robustness**: Always handle API error responses and map them to `ModelState` in the Client controllers.
4. **Consistency**: Use the existing DTOs for data transfer.
5. **Testing**: Always run existing tests before and after changes. If you implement new features or modify critical logic, generate corresponding tests in `Modulos.Tests`.
6. **Confirmation Guards**: ALWAYS implement a confirmation dialog (`confirm()`) before executing sensitive or destructive actions (e.g., deleting a user, resetting a password, or changing a role).

---
*Generated by Antigravity on 2026-03-13*
